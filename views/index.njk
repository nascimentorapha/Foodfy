{% extends "layout.njk" %}

{% block content %}

    
    <div class="wrapper">
            <h2>As melhores receitas</h2>
            <p>Aprenda a construir os melhores pratos com receitas
                criadas por profissionais do mundo inteiro.
            </p>
            <div class="img-chef">
                <img src="/chef.png" alt="Chefe">
            </div>
    </div>

    <div class="content">
        <h2>Mais acessadas</h2>
    </div>
    <section class="cards">
        
        {% for contents in content %}

        <div class="card" id="{{contents.title}}">
            <div class="card_image_container" >
                <img src="{{contents.image}}" alt="Imagem de {{contents.title}}">
            </div>
            <div class="card_content">
                <h2>{{ contents.title }}</h2>

            </div>
            <div class="card_info">
                <p>por {{contents.author}}</p> 
            </div>
        </div>

        {% endfor %}
        
    </section>

    <div class="modal-overlay">
        <div class="modal">
            <a class="close-modal">Fechar modal</a>         
            </div>

            <div class="modal-content">
                <img src="" alt="Imagem"></img>
            </div>
        </div>

    </div>

    
    <script src="scripts.js"></script>

{% endblock %}